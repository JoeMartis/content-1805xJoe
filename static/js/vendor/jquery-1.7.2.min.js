//To have the globally attached JQuery available
(function (requirejs, require, define) {

  define('jquery', function(){
    return window.jQuery;
  });

}(RequireJS.requirejs, RequireJS.require, RequireJS.define));